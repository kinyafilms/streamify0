/** @type {import('next').NextConfig} */
const nextConfig = {
  // In Next.js 16, Tailwind v4 is supported natively via this flag
  experimental: {
    tailwind: true,
  },
};

export default nextConfig;
